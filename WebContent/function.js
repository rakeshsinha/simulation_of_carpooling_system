//Constructing all the users

//Script.load("myBigCodeLibrary.js");

//Algorithm 1 (User Interface Agent and Information Agent's Process)

function InformationAgent(users) {
	var timeStamp = (new Date()).toString();
	//Extract Itenaries (Implement later)
}


//Algorithm 2 (Locating Agent)

vehicles = [
	{}
	];

function LocatingAgent() {
	var center = {x:5, y:5};
	var diameter = 5;
	var i, distance;
	
	return listVehicles;
}


//Algorithm 3 (Parameter Calculation and Graph Definition)

function GraphDef(){
	var w1, w2;
	var listVehicles = LocatingAgent();
	var NV = listVehicle.length;
	var w1 = 1-NV/S;
	var w2 = NV/S;
	
	//Gzi(Nzi, Vzi) Construct graph details (Cars Itineraries Intersection)
	//D3A (Gzi, ω1, ω2);
}

function opennewtab(url){
    var win = window.open(url, '_blank');
}